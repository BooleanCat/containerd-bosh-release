export PATH="${PATH}:/var/vcap/packages/runc/bin"
export PATH="${PATH}:/var/vcap/packages/containerd/bin"

log_dir=/var/vcap/sys/log/containerd
run_dir=/var/vcap/sys/run/containerd

root_dir="<%= p('containerd.root') %>"
state_dir="<%= p('containerd.state') %>"
address_dir="$( dirname "<%= p('containerd.grpc.address') %>" )"
debug_dir="$( dirname "<%= p('containerd.debug.address') %>" )"
